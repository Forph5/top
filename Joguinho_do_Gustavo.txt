COPIE O CODIGO A SEGUIR EM UM TERMINAL NA LINGUAGEM C






#include <stdio.h>7
int main(void) {

  int n1, n2, result;
  
do {
  printf("Digite um número: ");
  scanf("%d", &n1);
  printf("Digite outro número: ");
  scanf("%d", &n2);
  printf("A soma dos números é: %d\n", n1 + n2);

result = n1 + n2;
  
  if (result < 50, result != 50) {
     printf("Menor que o número desejado\nEscolha um número maior\n");
  }
  if (result > 50, result < 100, result != 50) {
    printf("Maior que o número desejado\nEscolha um número menor\n");
  }
  if (result > 100, result != 50) {
    printf("Número alto pra krl\nEscolha um número bem menor\n");
  }
  if (result == 50) {
    printf("Parabéns, você acertou!");
  }
  
  } while (result != 50);

    return 0;
}